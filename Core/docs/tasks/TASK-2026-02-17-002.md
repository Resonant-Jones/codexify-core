# TASK-2026-02-17-002 — Seed/Sync Provider Rows from `/api/llm/catalog`

- **Task:** TASK-2026-02-17-002 — Seed/Sync Provider Rows from `/api/llm/catalog`
- **Implementation Commit:** `134db821`

## Summary
- Added provider state sync helpers that derive launch-scope provider rows from the catalog payload.
- Implemented idempotent upsert logic for `inference_providers` and first-time initialization for `inference_provider_runtime`.
- Added PgDB support to sync provider rows from `build_llm_catalog(include_all=True)` and validate provider-state tables exist.
- Wired startup to run provider row sync without blocking app boot on errors.
- Added tests that verify launch-scope row derivation and idempotent re-sync behavior.

## Files Changed
- `guardian/core/provider_state.py`
- `guardian/core/pgdb.py`
- `guardian/guardian_api.py`
- `tests/core/test_provider_state_sync.py`

## DB/Migrations
- No new migration in this task.
- Uses existing provider-state tables from TASK-2026-02-17-001:
  - `inference_providers`
  - `inference_provider_runtime`

## API/Endpoints
- No new endpoints.
- Startup process now syncs provider ledger rows using the same catalog source as `/api/llm/catalog`.

## Behavior Changes
- On startup, provider config/runtime rows are seeded/synced idempotently from the catalog model.
- Sync is launch-scoped to provider IDs: `openai`, `anthropic`, `gemini`, `groq`, `local`.
- Provider row `enabled` reflects catalog authorization/availability policy.
- Runtime rows are created with `health_status='unknown'` and are not reset on subsequent sync runs.

## Tests
- `pytest tests/core/test_provider_state_sync.py -q` -> **pass** (`2 passed`)
- `pytest tests/core/test_inference_provider_schema.py -q` -> **pass** (`2 passed`)
- `pytest tests/routes/test_llm_catalog.py -q` -> **pass** (`4 passed`)

## Notes/Risks
- Catalog currently includes `minimax`, but this sync intentionally seeds only the 5 launch providers from campaign scope.
