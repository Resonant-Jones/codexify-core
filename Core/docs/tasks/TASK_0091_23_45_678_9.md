# Task 009 — Tooling/Docs: Deterministic Doc-Gen Validation Artifact (FINDING-2026-02-16-009)

Preflight: git status --porcelain -uall must be empty

## STOP Conditions
1) If preflight is not empty, STOP and run:
- `git status --porcelain -uall`
- `git restore --staged --worktree -- .`
- `git clean -fd`

2) If any out-of-scope files appear at any point, STOP and run:
- `git status --porcelain -uall`
- `git restore --staged --worktree -- .`
- `git clean -fd`

## Finding
- ID: `FINDING-2026-02-16-009`
- Severity: `INFO` (map to task risk: LOW)
- Title: Core loop status: doc-gen (generate → persist → thread documents list) — closed; add deterministic validation

## Outcome (must be observable)
- A deterministic validation artifact exists (doc/script) that verifies:
  1) Authenticated call to `/api/documents/generate` returns `document_id` + `content`
  2) `/api/threads/{thread_id}/documents` lists the generated document
  3) Notes what to do if `thread_id=1` does not exist (create a thread via documented OpenAPI endpoint)

## Allowed Files (strict)
- `docs/**/*.md`
- `scripts/**/*.sh`
- `scripts/**/*.py`

## Dependencies / Prereqs (deterministic checks)
- `docker --version`
- `docker compose version`

## Command Checklist
1) Preflight:
- `git status --porcelain -uall`

2) Implement artifact using audit-suggested commands:
- Include:
  - `docker compose up -d db redis backend`
  - `curl -sS -H "X-API-Key: $GUARDIAN_API_KEY" -H "Content-Type: application/json" -d '{"thread_id":1,"prompt":"Write a short audit note.","format":"markdown"}' http://localhost:8888/api/documents/generate`
  - `curl -sS -H "X-API-Key: $GUARDIAN_API_KEY" http://localhost:8888/api/threads/1/documents`
- Include explicit expected response fields and pass/fail criteria.

3) Scope check:
- `git status --porcelain -uall`

## Expected Outputs (success signals)
- The repo contains a deterministic doc/script for doc generation validation.
- The artifact explicitly describes expected JSON fields and how to handle missing threads.
- `git status --porcelain -uall` shows modifications only within Allowed Files.

## Rollback / Cleanup Commands
- `git restore --source=HEAD --staged --worktree -- docs`
- `git restore --source=HEAD --staged --worktree -- scripts`
- `git clean -fd`


## Runner Receipt (Start)

- Campaign: CAMPAIGN_2026_02_16_COMPILED_AUDIT

- Task ID: 009

- Head before: 41bbc8d390dafb12ab64b28e1074821e94643acf


## Completion Summary (Runner)

- Status: success

- Summary: Doc-gen runbook + script ready; run scripts/validate_doc_gen.sh to verify doc loop.

- Implementation commit hash: 4605734eadab3222c3b435485c0dfb9316494e6d

- Receipt update commit hash: 72aea0e90bef7fd5fe572c98c83b650b452a8193

- Tests ran: Preflight clean: git status --porcelain -uall

- Notes: Script not executed (needs Docker + backend stack).

<details>
<summary>Structured task_result.json</summary>

```json
{
  "status": "success",
  "summary": "Doc-gen runbook + script ready; run scripts/validate_doc_gen.sh to verify doc loop.",
  "tests_ran": [
    "Preflight clean: git status --porcelain -uall"
  ],
  "commit_hash": "4605734eadab3222c3b435485c0dfb9316494e6d",
  "implementation_commit_hash": "4605734eadab3222c3b435485c0dfb9316494e6d",
  "receipt_update_commit_hash": "72aea0e90bef7fd5fe572c98c83b650b452a8193",
  "notes": "Script not executed (needs Docker + backend stack)."
}
```

</details>
