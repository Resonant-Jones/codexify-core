# Chat Runtime State Contract

This filename is retained as a compatibility pointer after the merge with `main`.

The normative document is `docs/architecture/chat-runtime-contract.md`.

Use that file for:
- canonical provider runtime states
- canonical request lifecycle states
- message-versus-attempt identity
- UI status presentation rules
- replay and transition semantics

This pointer should not diverge from the canonical contract.
