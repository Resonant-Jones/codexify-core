# TASK-2026-02-17-001 — Add Provider State Schema (Config + Runtime)

- **Task:** TASK-2026-02-17-001 — Add Provider State Schema (Config + Runtime)
- **Implementation Commit:** `e3d1cdb7`

## Summary
- Added Alembic revision `62127ee9a537` off head `a7c9d1e2f3b4`.
- Created `inference_providers` table with config fields (`enabled`, `priority`, `default_model_id`, `capabilities`, `metadata`) and timestamps.
- Created `inference_provider_runtime` table with runtime health fields, FK to `inference_providers`, and runtime metrics/timestamps.
- Added required indexes: `ix_inference_providers_enabled`, `ix_inference_providers_priority`, `ix_inference_provider_runtime_health_status`.
- Added ORM models (`InferenceProvider`, `InferenceProviderRuntime`) aligned to migration schema.
- Added focused schema tests for columns/indexes/constraints and SQLite table creation.

## Files Changed
- `guardian/db/migrations/versions/62127ee9a537_add_inference_provider_state_tables.py`
- `guardian/db/models.py`
- `tests/core/test_inference_provider_schema.py`

## DB/Migrations
- New Alembic migration: `62127ee9a537_add_inference_provider_state_tables.py`.
- New tables:
  - `inference_providers`
  - `inference_provider_runtime`
- New indexes:
  - `ix_inference_providers_enabled`
  - `ix_inference_providers_priority`
  - `ix_inference_provider_runtime_health_status`
- Added runtime health and numeric guard check constraints.

## API/Endpoints
- None.

## Behavior Changes
- Database schema now supports explicit inference provider configuration state and separate provider runtime health state.
- ORM metadata now includes provider state tables, enabling downstream routing/health tasks to use typed models.

## Tests
- `pytest tests/core/test_inference_provider_schema.py -q` -> **pass** (`2 passed`).
- `pytest tests/test_migrations.py -q -rs` (host) -> **skip** (host `DATABASE_URL` points to container hostname `db` and cannot connect from host).
- `docker compose run --rm --entrypoint sh backend -lc "alembic -c /app/backend/alembic.ini upgrade head"` -> **pass** (migration chain applies in containerized Postgres environment).
- `docker compose run --rm --entrypoint sh backend -lc "pytest tests/test_migrations.py -q -rs"` -> **fail** due pre-existing unrelated schema-contract mismatch: missing `project_document_links` after full migration chain.

## Notes/Risks
- Existing baseline migration/metadata contract issue (`project_document_links`) is outside TASK-2026-02-17-001 scope and remains unresolved.
