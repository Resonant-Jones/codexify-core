Replace image generation stub with real provider call (Feature 5 core)

Context:
You’re operating on the local Codexify repo. Each task must be self-contained, testable, and committed individually.

Instructions:

1. Perform the described edit only in the specified files:
   - guardian/routes/media.py
   - (optional) guardian/image_gen/router.py or provider modules ONLY if required
   - add/extend backend tests for the endpoint behavior
2. Run backend tests:
   pytest -v
3. If tests pass:
   git add guardian/routes/media.py <test file(s)>
   git commit -m "Implement real image generation via provider router"
4. Output:
   - Summary
   - Test results
   - Git commit hash

Task Description:
Implement /api/media/generate/image so it calls the configured provider, saves the generated bytes to storage, and returns a real src_url.

Acceptance Criteria:

- Endpoint no longer returns a fake /media/generated/{id}.png unless that file actually exists
- Uses IMAGE_GEN_PROVIDER env var with a sane default
- Saves generated image bytes to the configured storage backend
- Returns src_url that can be fetched by the frontend without 404
- Adds at least one test with a mocked provider that asserts storage save + response url
