# Task 008 — Tooling/Docs: Deterministic Image-Gallery + Image-Gen Validation Artifact (FINDING-2026-02-16-007, FINDING-2026-02-16-008)

Preflight: git status --porcelain -uall must be empty

## STOP Conditions
1) If preflight is not empty, STOP and run:
- `git status --porcelain -uall`
- `git restore --staged --worktree -- .`
- `git clean -fd`

2) If any out-of-scope files appear at any point, STOP and run:
- `git status --porcelain -uall`
- `git restore --staged --worktree -- .`
- `git clean -fd`

## Findings
- ID: `FINDING-2026-02-16-007` (INFO, task risk: LOW)
- ID: `FINDING-2026-02-16-008` (INFO, task risk: LOW)
- Theme: image-gallery + image-gen loops depend on `/media` URLs being fetchable

## Outcome (must be observable)
- A deterministic validation artifact exists (doc/script) that validates:
  1) Gallery list endpoints return items for `tag=uploaded` and `tag=generated`
  2) Image generation endpoint creates a persisted record that appears in `tag=generated` list
  3) Returned `src_url` values are fetchable in the same runtime (depends on Task 003)

## Allowed Files (strict)
- `docs/**/*.md`
- `scripts/**/*.sh`
- `scripts/**/*.py`

## Dependencies / Prereqs (deterministic checks)
- `docker --version`
- `docker compose version`

## Command Checklist
1) Preflight:
- `git status --porcelain -uall`

2) Implement artifact using audit-suggested commands:
- Include:
  - `docker compose up -d db redis backend`
  - `curl -sS -H "X-API-Key: $GUARDIAN_API_KEY" "http://localhost:8888/api/media/images?tag=uploaded&limit=5"`
  - `curl -sS -H "X-API-Key: $GUARDIAN_API_KEY" "http://localhost:8888/api/media/images?tag=generated&limit=5"`
  - Generation example:
    - `curl -sS -H "X-API-Key: $GUARDIAN_API_KEY" -H "Content-Type: application/json" -d '{"prompt":"audit test image","model":"dall-e-3","project_id":1,"thread_id":1,"user_id":"default"}' http://localhost:8888/api/media/generate/image`
- Explicitly state provider dependencies (e.g., `LLM_PROVIDER`) and pass/fail criteria.

3) Basic sanity checks:
- `curl -sS -H "X-API-Key: $GUARDIAN_API_KEY" "http://localhost:8888/api/media/images?tag=uploaded&limit=5" || true`
- `curl -sS -H "X-API-Key: $GUARDIAN_API_KEY" "http://localhost:8888/api/media/images?tag=generated&limit=5" || true`

4) Scope check:
- `git status --porcelain -uall`

## Expected Outputs (success signals)
- The repo contains a deterministic doc/script covering list + generate + gallery refresh verification.
- The artifact clearly documents required env vars and expected JSON response fields.
- `git status --porcelain -uall` shows modifications only within Allowed Files.

## Rollback / Cleanup Commands
- `git restore --source=HEAD --staged --worktree -- docs`
- `git restore --source=HEAD --staged --worktree -- scripts`
- `git clean -fd`


## Runner Receipt (Start)

- Campaign: CAMPAIGN_2026_02_16_COMPILED_AUDIT

- Task ID: 008

- Head before: 38eae958ee92a80004b18e822a4387cebe638abf


## Completion Summary (Runner)

- Status: success

- Summary: Deterministic validation doc + automation script added; prereq commands and sanity curls executed (failed gracefully because backend was offline).

- Implementation commit hash: a3e3b82306999234d74c9158f28dc773534ff790

- Receipt update commit hash: 9808d061312937b677d89ee4fa5c541415233b61

- Tests ran: docker --version, docker compose version, curl -sS -H "X-API-Key: $GUARDIAN_API_KEY" "http://localhost:8888/api/media/images?tag=uploaded&limit=5" || true, curl -sS -H "X-API-Key: $GUARDIAN_API_KEY" "http://localhost:8888/api/media/images?tag=generated&limit=5" || true

- Notes: curl probes failed with connection refused because backend was not running, which is expected until services are brought up.

<details>
<summary>Structured task_result.json</summary>

```json
{
  "status": "success",
  "summary": "Deterministic validation doc + automation script added; prereq commands and sanity curls executed (failed gracefully because backend was offline).",
  "tests_ran": [
    "docker --version",
    "docker compose version",
    "curl -sS -H \"X-API-Key: $GUARDIAN_API_KEY\" \"http://localhost:8888/api/media/images?tag=uploaded&limit=5\" || true",
    "curl -sS -H \"X-API-Key: $GUARDIAN_API_KEY\" \"http://localhost:8888/api/media/images?tag=generated&limit=5\" || true"
  ],
  "commit_hash": "a3e3b82306999234d74c9158f28dc773534ff790",
  "implementation_commit_hash": "a3e3b82306999234d74c9158f28dc773534ff790",
  "receipt_update_commit_hash": "9808d061312937b677d89ee4fa5c541415233b61",
  "notes": "curl probes failed with connection refused because backend was not running, which is expected until services are brought up."
}
```

</details>
